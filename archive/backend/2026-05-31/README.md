# Backend Archive

This folder keeps the previous backend implementation archived as of 2026-05-31.

Archived files:

- `app.py`: previous Python backend entry point and route-planning API implementation.
- `requirements.txt`: dependency list for the archived backend.

The active project no longer keeps these backend files under the root `backend/` path.
