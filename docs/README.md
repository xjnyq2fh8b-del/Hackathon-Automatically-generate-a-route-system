# Docs

本目录用于存放项目产品规则、算法规则、接口约定、交付说明等长期维护文档。

命名规则：

- 使用小写英文和连字符命名，例如 `routing-rules.md`。
- 同一主题只保留一份主文档，历史版本如需保留，放入后续单独建立的 `docs/archive/`。
- 临时草稿不放入本目录，确认进入项目规则后再沉淀为正式文档。

清理规则：

- 过期内容应合并到主文档或移入归档目录。
- 与路线规划 Agent 无关的资料不放入本目录。
